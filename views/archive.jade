if posts.length > 0
        div#content
            section
                h2.archive Archive
                ul.archive
                    each post in posts
                        li
                            a(href='#{post.url}')= post.title
                            span= post.date

else
    p.nothingyet No entries yet.